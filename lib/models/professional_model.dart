class ProfessionalModel {
  String id;
  String nome;
  String descricao;
  String foto;
  int likes;
  List serviceType;
  String phoneNumber;

  ProfessionalModel(this.id, this.nome, this.descricao, this.foto, this.likes,
      this.serviceType, this.phoneNumber);
}
